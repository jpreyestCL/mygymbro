# graphify knowledge graph — derived from the code, rebuilt by the post-commit hook.
# Regenerate from scratch with: graphify extract . --code-only && graphify cluster-only .
graphify-out/

# Build output and dependencies
node_modules/
dist/

# Agent tooling: machine-specific absolute paths, not shareable
.claude/
.claude-flow/

.DS_Store
api/node_modules/

# A clone of the upstream project (gitlab.com/DuarteSantos8/opengym), kept alongside for
# comparing against this fork. It carries its own .git, so committing it would land a broken
# gitlink rather than the files.
opengym/
